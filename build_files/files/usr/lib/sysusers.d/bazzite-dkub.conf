# systemd sysusers.d configuration for bazzite-dkub groups
# This declares groups for proper immutable OS integration
#
# See: systemd-sysusers(8) and sysusers.d(5)

#Type Name              ID    GECOS                           Home  Shell
g     ccache            -     -                               -     -
g     docker            -     -                               -     -
g     onepassword       -     -                               -     -
